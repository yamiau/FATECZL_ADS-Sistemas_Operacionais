package controller;

public class NetworksController {
	public String IP(String os) {
		
		if (os.contains("win")) {
			
		} else {
			
		}
		
		return(os);
	}
	
	public String ping(String os) {
		
		if (os.contains("win")) {
		
		} else {
			
		}

		return(os);
	}
}
